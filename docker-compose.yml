version: '3'
services:
  web:
    build: .
    image: antoinert/tonto:latest
    ports:
    - "3000:3000"
    depends_on:
    - mongo
    volumes:
    - .:/usr/src/app
  mongo:
    image: mongo
    ports:
    - "27017:27017"
    volumes:
    - mongo-backup:/data/db

volumes:
  mongo-backup:
